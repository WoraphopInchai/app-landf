# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
devlog.txt

node_modules
dist
dist-ssr
*.local

# Firebase
.firebase/
.firebaserc.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files
.env
.env.*

# Vercel
.vercel

# Firebase service account (ห้าม commit ขึ้น git โดยเด็ดขาด)
firebase-admin-key.json
firebase-admin-key.json.*

# ไฟล์ช่วยคัดลอก env (มีคีย์ลับ — ห้าม commit)
vercel-env-template.txt

# Load test ไฟล์ (ไม่เกี่wกับแอปจริง)
loadtest*.js
report.html

# เอกสาร deploy เดิม (ล้าสมัย)
DEPLOY_CHECKLIST.md
